node_modules/
dist/
site/studio-dist/
*.log
.DS_Store
Thumbs.db

# Atlas local ledger + observed cuts (contains private repo heads — never commit)
workspace/
# Build-worker outputs (regenerated; projection source lives in site/atlas-projection.json)
site/atlas/projection.json
site/atlas/snapshots/
# Brand OS build inputs (regenerated by scripts/sync-brand.mjs)
site/.brand/
