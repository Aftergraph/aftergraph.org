# Spike preservation (14-repo static dashboard)

Copied 2026-09-08 from `C:/Users/empir/aftergraph-site/` (`index.html`, `data.json`).

Status: VISUAL SPIKE ONLY. It renders a stale 14-of-25 repository subset with
hand-frozen data and no provenance. It is not a truth source, not the product,
and must not be extended. Atlas (`site/atlas.html` + generated
`site/atlas-projection.json`) supersedes it as the development observatory.
